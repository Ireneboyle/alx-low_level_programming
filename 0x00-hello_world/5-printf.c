#include <stdio.h>

/**
 * main - prints exactly the phrase  with proper grammar,
 * but the outcome is a piece of art
 * Return: 0 if done properly
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
